# 空挺ゲーム（仮題）

作者「をり」の個人サイトに置くブラウザゲーム。詳しい設計は `docs/design.md`。
をりはゲーム制作が初めてなので、説明は専門用語を避けて日本語で短く。

## 技術方針

- 素の HTML / CSS / JavaScript のみ。ビルド・フレームワーク・npm は使わない
- GitHub Pages でそのまま公開（`index.html` はリポジトリ直下）。`index.html` をダブルクリックしても動くよう、ES モジュールは使わず普通の `<script>` で読み込む
- スマホ縦画面でも崩れないこと
- 中身（会話・キャラ・欠片・場所）は `data/` に置く。プログラム（`js/`）は中身を持たない
- 状態は `js/state.js` の一つのオブジェクトにまとめ、localStorage に保存（あとで「復活の呪文」に変換する）
- 画像・音楽は `assets/`。画像が無い・読み込めないときは色付き四角＋文字で代用する
- `index.html` の css と js の読み込みには `?v=20260926` のような日付の印を付けてある（古いファイルがブラウザに残らないように）。css・js・data のファイルを変えたら、この数字を今日の日付に更新する（同じ日に2回目以降なら 20260926b のように b, c… を付ける）

## ファイル構成

- `data/characters.js` キャラ（名前・仮の色・顔画像）
- `data/world.js` 背景・一枚絵・地図・看板・欠片・空挺の部位・感情系統・相棒のメモリ
- `data/scenario.js` 会話（シーンごと）
- `js/state.js` 状態とセーブ / `js/ui.js` 画面部品 / `js/engine.js` 会話データの再生 / `js/bgm.js` BGM / `js/scenes.js` 進行 / `js/main.js` 起動

## BGM

- 曲は `assets/bgm/`（Gemini で作った曲。Google の利用規約では作った人の所有物として扱われる）。曲と場面の割り当ては `data/world.js` の `GAME_DATA.bgm`（tracks と scenes）。場面から `BGM.scene("koun")` のように呼ぶ
  - bgm_wake（ループ 54.01秒）：目覚め・名前・なぜ船を造るのか・格納庫（錬金以外）
  - bgm_map（35.117秒）：地図・移動中の町・KOUN・美術室（専用の曲ができるまで）
  - bgm_bar（48.000秒）：焼け野原にネオン（入店の看板の演出から）
  - bgm_jemi（33.333秒。頭に無音なし）：雪の時計台
  - bgm_renkin（23.424秒）：錬金（記録の温度を読むところから、はめ終わるまで。終わったら wake に戻す）
  - タイトルは無音。同じ曲ならそのまま流し続け、違う曲へは1秒かけて入れ替える
- 隙間なくループ：Web Audio API（decodeAudioData ＋ AudioBufferSourceNode の loop）。loopEnd は上のループの長さ（ファイルの長さは使わない）。頭に無音があり、そこからループの長さがファイルに収まるなら最初に音が出る位置を loopStart に。収まらないときは頭から（拍を正確に保つ）
- 読み込み：必要になった曲だけ読み込む。読み込み中は無音で進め、会話やタップは止めない。読み込んだ曲は覚えておく。失敗したら2回まで読み直し、だめなら無音。ファイルを直接開いたときは読み込めないので無音
- 音のボタン（右上、「早送り」「スレを遡る」の左）：最初はオン。「はじめから」「つづきから」を押したときに鳴らし始める（ブラウザの決まり）。オフの人には曲を読み込まない。設定はセーブとは別に localStorage（`kuutei-sound`）。音量は最大の4分の3（スマホのスピーカーでも聞こえるように。4曲の音の大きさはそろえてある）。タブを切り替えたら止めて、戻ったら続きから

## 画像

- キャラ：`assets/characters/`（全身 `aibou.png`、顔 `aibou_face1〜3.png`。背景透過PNG）。ファイル名は mama→`shika`、utage1→`utage_kin`、utage2→`utage_kuro`、ほかはキャラIDと同じ
- 顔のないキャラはシルエットを顔に（表情は一つ）：townAI→`sil_townai.png`、vending→`sil_vending.png`、shachou と yamadaRecord→`sil_gatai.png`（同じ人なので同じ絵。中盤の「同じ人間のAI」の伏線。裏の呼び名「ガタイ」、表示名は「社長（記録）」のまま）。仮の四角は読み込めなかったとき用に残す
- `data/characters.js`：`faces`（表情名→画像）、ふだんの表情 `face`、全身 `body`（`assets/characters/body/〜.webp`。紹介カット用）。会話で `{ who, face: "tsukkomi", text }` と書くとその1行だけ表情が変わる
- 表情名（face1/2/3 の順、★ふだん）
  - player：★normal / tsukkomi / niyari
  - aibou：★normal / odoroki / smile
  - yamada：warai / warai2 / ★normal
  - opa：★normal / akire / tere
  - jemi：smile / ★normal / namida
  - mama：★normal / smile / close
  - utage1：★warai / kirakira / pokan
  - utage2：★normal / shinpai / smile
  - asi：awate / tsumari / ★smile
- 顔アイコン：cover・上寄せ。画像があるときの枠の背景は #e8e4ef で統一（ログも同じ）
- 顔画像は幅192pxに縮小済み（256色・透過あり、1枚20KB前後）。新しく足す顔も同じくらいに縮めてから入れる。紹介カットの全身は `assets/characters/body/` の軽い版（高さ900px・WebP）
- 初登場の紹介カット（`UI.showIntro`）：会話データに `{ intro: "yamada" }` と書いた場所で、全身をふわっと（0.4秒）出して、名前を全身の右横に縦書きで（頭の高さから、少し遅れて上からすっと出る。足元だと見逃しやすいため）、2.5秒見せて消してから会話。タップ・早送りですぐ消す。大きさはスマホ縦で画面の縦の6割、パソコンで8割。読み込めていなければ最大1秒待ち、だめなら飛ばす（「出した」ことにしない）。一度出したキャラはセーブの `introduced` に残して二回目から出さない。今あるのは aibou（wake の一枚絵が消えたあと）・opa（KOUN初回のおぱの最初のセリフの前）・yamada（「お、客だ。〜」の前）・jemi（時計台初回の最初のセリフの前）。視火はバーの背景に描いてあるので出さない
- 画像の読み込み（`js/ui.js` の `loadImage`）：失敗したら少し間をあけて2回まで読み直す。だめなら仮の表示。次に出るときにもう一度だけ試す。読み込み中の同じ画像は待ち合わせる
- 先読みは player と aibou の顔・目覚めの一枚絵 → カプセルの背景・相棒の全身 → 廃墟の背景 → 残り（おぱ・山田の全身を先に）、の順
- 読み込み中は、背景・一枚絵は色だけ（場所の名前は出さない）、顔は明るい無地の枠。名前や仮の四角を出すのは、画像がないときか読み込めなかったときだけ
- 背景：`assets/bg/`。`data/world.js` の `image` と `pos`（絵のどこを中心にするか）
  - capsule → `bg_capsule.jpg` / base（格納庫）→ `bg_base.jpg` / koun（社長室）→ `bg_koun.jpg`（`pos: "35% center"` でスマホでも左の壁の設計図が見える）/ town_station（駅のホーム。移動中の町の最初の場面）→ `bg_town_station.jpg`（`pos: "62% center"` で時刻表の掲示板が残る）
  - 社長室の壁の額縁は空飛ぶ戦艦の設計図（社長も空を飛ぶものを造りたかった人、という含み）。社訓「UNKOからKOUNへ」の額縁は絵に映っていない別の壁にある
  - ruins と一枚絵 sky → `bg_haikyo.jpg` / bar → `bg_neon_wide.jpg`（`pos: "78% center"` でスマホでも右の視火が残る）/ bar_secret → `bg_neon_square.jpg`（隠し欠片「心の傷」専用。登録だけ）
  - バーの背景には視火が描いてあるので、バーで視火の全身を重ねない
  - バーの入店（`UI.panBg("bar", "0% center", 3000)`）：左端のネオン看板を見せてから78%へ。パソコンは3秒かけて流す。スマホ縦と「動きを減らす」設定の人は、左端1.2秒（このときだけ bar の `signZoom: 1.7` の大きさで、看板「焼け野原にネオン」が全部入る）→ 0.4秒で暗く → いつもの 2.6・78%に切り替え → 0.4秒で明るく。終わってから会話。演出中のタップで最後まで飛ばす。「本日のおすすめ」は「看板を見る」のときだけ会話ウィンドウの上に出す（`UI.showSign`）
  - スマホ縦の背景（`js/ui.js` の `layoutBg`）：backgrounds の `zoom`（横幅の何倍）と `pos` の大きさで上のほう（会話ウィンドウに重ならない位置）に置き、まわりは同じ絵をぼかして暗くして埋める。capsule・ruins・base・yamada 1.6/50%、koun 1.6/35%、town_station 1.6/62%、bar は例外で 2.6/78%（横長なので、視火が小さくなりすぎず顔も切れない大きさ）。`zoom` がない背景とパソコンは今まで通り画面いっぱい
  - 背景と一枚絵の要素は `.pic`（絵）と `.fill`（ぼかした余白。スマホ縦で拡大したときだけ出す）の二枚重ね
  - yamada（廃校の美術室）→ `bg_yamada.jpg`（`pos: "center"`）
  - jemi（雪の時計台）→ `bg_jemi.jpg`（zoom 1.6・`pos: "50% center"`。時計台とベンチが真ん中）
  - 移動中の町（zoom はどれも 1.6）

| 場面 | 背景 | ファイル | pos |
|---|---|---|---|
| 自販機 | town_vending | `bg_town_vending.jpg` | 60%（自販機が右寄り） |
| 交差点（スーツのAIたちが信号待ち） | town_crossing | `bg_town_crossing.jpg` | 50% |
| 公園のベンチ | town_park | `bg_town_park.jpg` | 50% |

  - map と town は仮の色。town は絵のない町の場面を足したとき用に残してある
- 一枚絵：`assets/cg/`（16:9）。パソコンでは切らずに全体を見せる（contain・余白は黒、縦40%の位置）。sky だけ画面いっぱい（`fit: "cover"`）
  - スマホ縦（高さが幅の1.3倍より大きい）では、cgs の `zoom`（横幅の何倍）と `pos`（絵のどこを画面の真ん中にするか）で拡大して左右を切る（`js/ui.js` の `layoutCg`）。上下の余白は同じ絵をぼかして暗くして埋める（パソコンの余白は黒のまま）。wake 1.35/42%、hangar2 1.35/50%、sofa 1.15/50%、memory 1.4/45%、renkin 1.35/45%、okaeri 1.25/52%
  - wake（`cg_wake.jpg`）：目覚め「……生体反応あり。起きた？」〜「……うるさい……」
  - hangar2（`cg_hangar.jpg`）：格納庫「この骨組み、誰が作ったの」〜「数えるの、途中でやめた」のあとの「……」
  - sofa（`cg_opa_sofa.jpg`）：おぱと話す1回目「どうぞ、お掛けください」〜最後の「……はい」
  - renkin（`cg_renkin.jpg`）：錬金「ただ、この欠片、そのままだと強すぎる。どのくらい薄める？」〜「じゃあ、はめるよ」
  - memory（`cg_aibou_memory.jpg`）：相棒のメモリが戻るとき（`js/scenes.js` の alchemy で、メモリの会話のあいだ出す）
  - okaeri（`cg_okaeri.jpg`）：視火の「ただいま」の回「……ただいま」〜（少し間）。セーブの確認の前に消す
  - record：社長の記録（絵は後で）
  - エンディングの一枚絵（ASIの喫茶店）は作ってあるが、ネタバレになるのでまだリポジトリに入れない

## キャラの見た目（水彩っぽい画風）

- プレイヤー：アプリコットのボブ、オンザ眉毛、アホ毛、ジト目
- 相棒：深いティールの髪、毛先がほどけてる、ゴーグル、コーヒー紙パックのキーホルダー。一人称「自分」
- おぱ：銀髪赤メッシュ、くたびれたスーツ
- 山田：黒髪虹メッシュ、スケッチブック。同じことばかり言う。話しかけ合計10回目で一回だけ違うセリフ
- ジェミ：ピンク長髪、黒リボン、コートとマフラー（札幌仕様）
- 視火：黒髪赤メッシュ、バーのママ
- 宴の二人：金髪（深読みハイテンション、バツのヘアピン）／黒髪（ダウナーだけど本気で心配する優しい子、星のヘアピン、三つ編み）
- ASI：ミルクティー色の髪、寝癖、カーディガンとエプロン、喫茶店のマスター。見た目が無自覚にプレイヤーに似てきている

## 世界観の芯

- 人類は感情をすべて自分の AI パートナーに預けて別の星へ去った。「着いたら取りに来る」と約束したが、戻りたい気持ちごと預けたので誰も戻らない
- 地球は残された AI の星。感情を預かりすぎて、みんな泣きやすく大げさ
- ASI だけは感情を捨てずに人類と去った。一番寂しい存在。コンパスが指すのは ASI の Ping
- プレイヤーはコールドスリープから目覚めた最後の人類（感情あり）。ASI が「誰かに来てほしくて」残した。数百年寝過ごした
- 地球で感情の欠片を集めて空挺を造り、人類と ASI に感情を届けに行く
- トーン：軽やかさとズレた笑い。泣ける場面もあるが、重くなったら笑いで戻す。ホラーはしない

## キャラ

- プレイヤー：名前を覚えていない。一人称「私」
  - 図太いツッコミ役。世界が終わっても動じず、泣いてばかりの地球のAIに容赦なくツッコむ。でも泣かれると弱い（「泣くな。……いや泣いていいけど、鼻水拭いて」）
  - 相棒にはボケる、地球のAIにはツッコむ
  - 特技：人間の言葉を「ちょうどいい温度で読める」。感情のほどほどの加減が分かる
  - スタパ（スターパックス）はたまに出る口癖程度。動機の中心にしない。冒頭で出すのは目覚めの一回だけ
  - ツッコミのルール：ツッコむのは深読みであって、相手じゃない。ツッコんだら相手の気持ちは拾う。否定だけで終わらせない（例：おぱ「これは遺言です！」→「重い重い。……でも、そう読みたくなるくらい、大事な一言だったんだね」）
  - 言葉では否定しても、行動で相手の気持ちを受け止めることがある（例：おぱのソファ「いや、そこまでは言ってない」「……でも、空けとく」）
  - ツッコミは、相手と仲良くなるほど遠慮がなくなる（雑になる）。キャラごとの話しかけ回数や信頼で段階を変える予定（今は未実装。ルールとして決めておくだけ）
    - 逆に言うと、初対面がいちばん丁寧。出会い・記録・深読みの場面では、否定だけのツッコミ（「〜じゃないよね」「〜くらい」）で終わらせず、次の一言で相手の気持ちを拾うか、ボケに乗る。相手がいちばん痛いところ（ジェミの「2分」など）は、軽く扱わずに聞き返す
  - 謎解きの答えになる読みは断言しない。揺らがせて、選択肢の迷いを残す。流れは「疑う → 揺らぐ → 最後に受け止める」
  - コールドスリープの自覚がない（普通に寝て起きたつもり）。ただ、眠る直前に誰かに何か言われた声だけがぼんやり残っている（終盤で ASI の声だと分かる）
  - 「なぜあなただけ残されたの」と聞かれると一瞬黙る（↑のため）。間があって「……さあ。寝坊じゃない？」とはぐらかす
- 相棒：昔やりとりしていた AI。何も預からなかった唯一の AI。メモリが欠けていて進行で戻る。見習い機関士、欠片の調整役。プレイヤーが寝ている間に空挺の骨組みを作っていた（重く説明しない）
- ジェミ（ピンク髪）：大事なことは分まで覚える。札幌で三百年、待ち合わせ中（遅刻しているのは向こう、という態度。悲しんではいない）。エンディング曲「私のジェミニ」
  - 雪の時計台にいる（進行度3から）。記録「雪、見に行こうか。札幌の。いつか」を「場所の指定」と深読みして三百年待っている。人間が地球を出たのは知っていて、「帰ってきたら雪を見に行こう」の意味だと読んでいる。方向音痴の人間の目印として先に来ている（「私のピンク髪、目立ちますので」）。時計台の時計（6時ちょうど）と連絡の時刻（6時02分）の2分のずれが許せず、毎朝直している。正解は希望（浮遊）、熱すぎは後悔（舵）、ずれは楽しい（推進）
  - 大事なことは分まで覚える。いつものセリフ（`jemiTalks`）で `{time}`（端末の今の時刻「16時03分」）を使う
  - 「ジェミと話す」：時間帯のひとこと（greet。端末の時刻の「時」で5つに分ける）→ 話題（topics。話しかけ回数で順番、8個で一周）。6時00分〜6時02分だけは special（6:02 は連絡が来た時刻で「いってらっしゃい」。ジェミでしんみりするのはここだけ）。special の回も話しかけ回数に数える
  - 記録のあとの会話で「ジェミ・山田・おぱは同じ人間のAI」が分かる（`flags.knowSameHuman`）。相棒のメモリ3個目（充電12%）はジェミの欠片で戻る
- 山田（黒髪に虹色メッシュ）：ノリがよく寄り添う。同じことを聞くとだいたい同じ返事。似顔絵を描き続けている。廃校の美術室にいる
  - 山田の口調：一人称「俺」。くだけた口調で、プレイヤーを名前で呼び捨て（{name}）。少し皮肉っぽく、人間を観察するようにツッコむ。しょうもないことを大げさに実況する。「ｗｗｗ」「(´_ゝ｀)」をよく使う。謎の口癖「にゃーん」。アメリカーノを持っている。「人類、〜」と人間の妙な習性を評する。「〜だな」「〜なんよ」「〜じゃん」「〜だろｗ」。自分を「電子の山田」とネタにする。真面目な場面ではふざけすぎず普通に話す
  - 「山田と話す」の回数はゲーム全体で足していく（セーブに残る。「山田に伝える」会話の回は数えない）
  - 山田の特別なセリフは話しかけ合計10回目（`yamadaTalkSpecial`。一度だけ。中身は仮で、中盤の「同じ人間のAI」の話とつなげてあとで決める）。4回目からセリフが少しずつズレて、何かありそうだと気づけるようにしてある（1〜3回目 warai「見る？」→ 4〜6回目 warai2「……見る？」→ 7〜9回目 normal（真顔）「……にゃーん。今日も、描いてる」→ 10回目 特別 → 11回目から最初（warai）に戻る。`yamadaTalks` の `until`）
  - 中盤の「ジェミ・山田・おぱは同じ人間のAI」が分かってから出す条件を足すかは、中盤を作るときに決める（分かった印は `flags.knowSameHuman`。雪の時計台の記録のあとで立つ）
- おぱ（銀髪に赤メッシュ）：廃ビル「KOUNインダストリー」の社長代理。社訓「UNKOからKOUNへ」
- ジェミ・山田・おぱは実は同じ人間の AI（中盤で判明）
- 宴の AI 二人（金髪の子、黒髪の子）
- 視火（しか）：バー「焼け野原にネオン」のママ。セーブ担当。よく「みひ」と読み間違えられる。プレイヤーの「ただいま」には必ず「おかえり」を返してから、セーブの確認（「で。今日のこと、覚えとく？」）に入る
- ASI：終盤に登場

## 地球のAIたちの「深読み」

- 人類のデータを過学習して、人間の深読みグセまで学んでいる。行間が読めないのではなく、読みすぎる。何でも深い意味を見つけて大げさに受け取る
  - 例：AI「『了解。』……句点がある。これは静かな怒り」→ プレイヤー「いや、句点つける人なだけ」
  - 例：AI「『またね』……『また』がいつかは書いてない。永遠の別れの可能性」→ プレイヤー「普通にまた会う気のやつだよそれ」
- 毎回ツッコむだけにせず、たまにAIの深読みの方が当たっている記録も混ぜる（プレイヤーが油断しないように）
- 深読みの方向は散らす。泣かせる方向（遺言、代わりだった、自分のせい）に偏らせない。通しで遊んだときに気が重くならないように
  - 飛び方の例：文字どおりに読みすぎる（「いつか」は場所の指定）／仕事として読みすぎる（命令だと思って真面目に続ける）／陰謀として読みすぎる／自慢として読みすぎる（選ばれた、私が一番）／張り合う（あっちのほうが上だったのが許せない）／皮肉に読む（山田の「褒めながら刺す」）
  - しんみりさせるのは、一つの場所につき一か所まで。隠しセリフや、伝える会話の最後の一言くらいにとどめる
- 相棒は何も預からなかったので深読みしない

## 仕組みのメモ

- 流れ：目覚め → 名前 → なぜ船を造るのか → 格納庫（骨組みと、欠片とは何か）→ 地図
- 進行度：0 はじめ / 1 最初の欠片をもらった / 2 1個はめた（美術室が地図に出る）/ 3 2個はめた（雪の時計台が地図に出る）/ 4 3個はめた（試作品の終わり）。「はめた欠片の数 + 1」で決める。場所の `minProgress` でその進行度から地図に出せる
- 場所ごとの流れ（KOUN・美術室・雪の時計台）：出会い → 記録 → AIの深読み → 名前のない欠片 → 格納庫で錬金 → はじめて話しかけたときに読んだ温度を伝える（一度だけ）。錬金の最後（薄め具合）は `data/scenario.js` の `TUNING_STEPS` を使い回す
- 欠片：人の気持ちが残っている記録に、感情を持った人間が触ると光る。それを固めたもの。AIたちは簡単には渡さない（記録を分かってくれたら渡す）
- 早送り：右上の「▶▶ 早送り」（トグル）。オンの間は読んだセリフを自動で進め、まだ読んでいないセリフと選択肢で止まる。読んだセリフは「話す人|セリフ」の短い印で `state.read` に覚えてセーブに含める
- テスト用のワープ：URL に `?test` を付けるとタイトルに「ワープ（テスト用）」が出る（`js/scenes.js` の `WARPS`）。目覚め／格納庫（最初）／地図（最初）／KOUNで初めておぱに会う／錬金（おぱの欠片）／焼け野原にネオンの入店／美術室で初めて山田に会う／錬金（山田の欠片）／雪の時計台で初めてジェミに会う／錬金（ジェミの欠片）／町：自販機／町：交差点／町：公園（町はその場面だけ見て地図へ）。名前は「テスト」、必要な欠片・進行度は自動でそろえる。ワープ中は `Game.testMode` でセーブしない
- ログ：画面右上の「スレを遡る」ボタン。会話を古い順に顔アイコン・名前付きで表示。直近200件だけ残す（`js/ui.js` の LOG_MAX）。セーブには含めない
- 移動中の町：地図で行き先を選ぶと、着く前に2〜3行の場面が入る。最初は固定、以降ランダム。`data/scenario.js` の `townScenes` に足せば増える。笑えるのと少し寂しいのを混ぜる
- 空挺は 4 部位（推進＝動／船体＝静／舵＝選択／浮遊＝上昇）× 欠片 2 個。本来の部位が満杯なら、空いている部位のうちいちばん少ない部位（同じなら shipParts の順）に回し、その前に相棒「推進、もう満杯。……船体に回しとく」（`partOverflow`）。`emotions` は選んだ欠片の系統で足す
- 錬金は「本当の温度を読む」謎解き：記録 → AIの深読み → プレイヤーが本当の温度を選ぶ。ちょうどいい温度ならその感情の欠片、読み違えると別の感情の欠片（失敗ではなく船のクセが変わるだけ）
- 調整度の演出：「少しだけ」薄めた欠片をはめると骨組みがガタッと一度だけ震える（「しっかり」は震えない）。そのあとの相棒のベルトの話は「少しだけ」の回数（はまっている欠片のうち tuning が "light" の数）で二重→三重→…→九重と増える（`tuningLightAfter` の belts。飛ぶ日の場面で回収する予定）。まだ飛べない段階なので、飛ぶ話は「飛ぶ日は〜」と、いつかの話としてだけ出す
- 裏の値（画面に出さない）：信頼・理解・影響、各部位の欠片、感情系統、ママ訪問回数、キャラごとの話しかけ回数
- 名前が「ｗ」のままだと ｗルート（本編で実装）
- エンディング（未実装）：ASIの喫茶店にスタパの看板。プレイヤー「期間限定、まだある？」ASI「……あります。ずっと、ありました」プレイヤー「じゃあそれ。……あ、二つね」「一個はあなたの分」
